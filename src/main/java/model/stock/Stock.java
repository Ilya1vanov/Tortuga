package model.stock;

import model.cargo.Cargo;
import model.port.Port;
import org.apache.log4j.Logger;

/**
 * Store {@link Cargo Cargos}.
 * @author Ilya Ivanov
 */
public class Stock {
    /* log4j logger */
    private static final Logger log = Logger.getLogger(Stock.class);
}
