package model.cargo;

import org.apache.log4j.Logger;
import model.ship.Ship;

/**
 * <p>Cargo, that is transporting by {@link Ship Ships}.</p>
 * @author Ilya Ivanov
 */
public class Cargo {
    private static final Logger log = Logger.getLogger(Cargo.class);
}
